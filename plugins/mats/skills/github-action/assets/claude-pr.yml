name: Claude Code (PR Review)

on:
  pull_request_review:
    types: [submitted]
  issue_comment:
    types: [created, edited]

concurrency:
  group: claude-pr-${{ github.event.pull_request.number || github.event.issue.number }}
  cancel-in-progress: false

jobs:
  claude:
    if: |
      (github.event_name == 'pull_request_review' &&
        github.event.review.state != 'approved' &&
        (github.event.pull_request.user.login == 'claude[bot]' ||
         contains(github.event.review.body, '@claude'))) ||
      (github.event_name == 'issue_comment' &&
        github.event.issue.pull_request &&
        contains(github.event.comment.body, '@claude'))
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
      issues: read
      id-token: write
      actions: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # CACHE_STEP
      # INSTALL_STEP

      - name: Check if already handled
        id: check
        run: |
          claude_last=$(gh api "repos/$REPO/issues/$NUMBER/comments" \
            --jq '[.[] | select(.user.login == "claude[bot]")] | sort_by(.updated_at) | last | .updated_at // empty')
          if [ -z "$claude_last" ]; then
            echo "skip=false" >> "$GITHUB_OUTPUT"
          elif [ "$(date -d "$claude_last" +%s)" -ge "$(date -d "$TRIGGER_AT" +%s)" ]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          NUMBER: ${{ github.event.pull_request.number || github.event.issue.number }}
          TRIGGER_AT: ${{ github.event.review.submitted_at || github.event.comment.created_at }}

      - name: Checkout PR branch
        if: steps.check.outputs.skip != 'true'
        run: gh pr checkout ${{ github.event.pull_request.number || github.event.issue.number }}
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Initialize comment tracking
        if: steps.check.outputs.skip != 'true'
        run: |
          gh api "repos/$REPO/issues/$NUMBER/comments" \
            --jq 'if length > 0 then last.id else 0 end' > /tmp/claude-last-comment-id
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          NUMBER: ${{ github.event.pull_request.number || github.event.issue.number }}

      - name: Load prompt
        if: steps.check.outputs.skip != 'true'
        id: prompt
        run: |
          prompt=$(sed "s|{{NUMBER}}|$NUMBER|g; s|{{REPOSITORY}}|$REPO|g" .github/prompts/pr-review.md)
          echo "content<<EOF" >> "$GITHUB_OUTPUT"
          echo "$prompt" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"
        env:
          NUMBER: ${{ github.event.pull_request.number || github.event.issue.number }}
          REPO: ${{ github.repository }}

      - name: Run Claude Code
        if: steps.check.outputs.skip != 'true'
        id: claude
        uses: anthropics/claude-code-action@v1
        env:
          ISSUE_NUMBER: ${{ github.event.pull_request.number || github.event.issue.number }}
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          additional_permissions: |
            actions: read
          prompt: ${{ steps.prompt.outputs.content }}
          claude_args: |
            --model claude-opus-4-6
            --permission-mode acceptEdits
            --allowedTools "Bash(gh pr view:*),Bash(bash .github/scripts/update-comment.sh:*),Bash(gh issue view:*),Bash(gh api repos/${{ github.repository }}/pulls/*/comments *),Bash(git add:*),Bash(git commit:*),Bash(git push origin HEAD)"
            --disallowedTools TodoWrite
